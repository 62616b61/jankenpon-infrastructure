apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: jankenpon-gateway-ingress
#  annotations:
#    kubernetes.io/ingress.global-static-ip-name: jankenpon-gateway-ip
spec:
  rules:
  - host: jankenpon
    http:
      paths:
      - backend:
          serviceName: jankenpon-gateway-service
          servicePort: 80
